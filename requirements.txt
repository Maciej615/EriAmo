# --- Rdzeń obliczeniowy ---
numpy>=1.21.0           # Obliczenia wektorowe (amocore, music_analyzer)
pandas>=1.3.0           # Obsługa historii duszy i migracja CSV (SoulStateLogger)

# --- EriAmo Union & Hardware ---
psutil>=5.8.0           # Digital Proprioception (odczyt CPU, RAM, temperatury)
colorama>=0.4.4         # Kolorowy interfejs w terminalu

# --- Moduł Muzyczny ---
music21>=7.3.0          # Teoria muzyki, generowanie partytur, analiza MIDI
midi2audio>=0.1.1       # Renderowanie audio (wymaga FluidSynth w systemie)
pydub>=0.25.1           # Konwersja audio (WAV -> OGG/FLAC)

# --- Przetwarzanie tekstu (z rdzenia AII) ---
unidecode>=1.3.0        # Normalizacja tekstu (dla modułów językowych)
